#ifndef STANDARDIZE_H
#define STANDARDIZE_H
#include <string.h>

using std::string;

inline bool Esc(const char&);
inline bool is_info(const string&);
//inline void openfile(std::ifstream&,const string&);

#endif
